diff a/sidecar/src/tailer.py b/sidecar/src/tailer.py	(rejected hunks)
@@ -60,9 +60,31 @@ class FileTailer:
 
         # 2. Shared Metadata Extraction (Regex/Parser logic)
         metadata = extract_log_metadata(self.workspace_id, self.filepath, line)
+        
         timestamp = metadata["timestamp"]
         level = metadata["level"]
 
+        # Additionally attempt dynamic custom parser via parser (PARS-003)
+        # Fetch configuration for this source
+        cursor = self.db.get_cursor()
+        try:
+            cursor.execute(
+                "SELECT parser_config FROM fusion_configs WHERE workspace_id = ? AND source_id = ?",
+                (self.workspace_id, self.filepath)
+            )
+            row = cursor.fetchone()
+            if row and row[0]:
+                import json
+                config = json.loads(row[0])
+                if hasattr(self.parser, "apply_custom_config"):
+                    custom_metadata = self.parser.apply_custom_config(line, config)
+                    if custom_metadata.get("timestamp"):
+                        timestamp = custom_metadata["timestamp"]
+                    if custom_metadata.get("level"):
+                        level = custom_metadata["level"]
+        except Exception as e:
+            print(f"[Tailer] Custom parser error: {e}")
+
         # 3. Persistence
         cursor = self.db.get_cursor()
         cursor.execute(
