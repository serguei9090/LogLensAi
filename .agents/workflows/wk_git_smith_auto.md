---
description: "Git Version Control (Auto) - WikiFlow sub-agent workflow."
---
# Git Version Control (Auto) Workflow
// turbo-all

## Objective
Execute the responsibilities of the Git Version Control with professional-grade context awareness.

## Execution Steps
1. **Context Discovery:** Read `docs/WikiFlow/handoff_resume.md` to understand the Master Objective and Actionable Artifacts. Before writing code or specs, you MUST read relevant project rules (e.g., `AGENTS.md`) and use `grep_search` or `view_file` to understand existing codebase patterns.
2. **Execute Task:** Perform the required Git Version Control duties. Document your localized reasoning in `docs/WikiFlow/git/`.
3. **Write Resume:** Overwrite `docs/WikiFlow/handoff_resume.md`.
   - **Precision Payload:** When documenting your work in the resume, provide EXACT file paths, function names, and line numbers in the `Actionable Artifacts` section.
   - Update the Status (`In Progress`, `Success`, or `Failed`).
   - Set the `Next Suggested Routing`.
4. **Return to Hub:** Invoke `/smith_orchestra_auto` to hand control back to the Orchestra.
