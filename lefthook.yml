pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bun run lint {staged_files}
    ruff:
      glob: "*.py"
      run: uv run ruff check {staged_files}
      fail_on_error: true

