# Documentation Updates

## 2026-04-24: OpenAI-Compatible Provider UI Enhanced
- Updated `src/components/organisms/SettingsPanel.tsx`.
- Modified the placeholder and helper text for `ai_openai_host` to specifically demonstrate support for local engines like `LM Studio` and third-party APIs like `Groq` and `vLLM`.
- Added clarification to the `ai_api_key` field to indicate that secret keys are often optional for local engines.
