# FEA-002: Global Temporal Filtering Implementation

## 📝 Status: In Progress
- **Assigned to**: @frontend, @backend
- **Sprint**: 04
- **Feature Spec**: `docs/features/global_temporal_filtering.md`

## 🎯 Task Breakdown

### 1. UI Migration (`FEA-002-UI-001`)
- **Move to Toolbar**: Locate `LogToolbar.tsx` and insert the `TimeRangePicker` component between the `SearchBar` and the `FilterBuilder`.
- **Logic**: It should bind directly to `timeRange` and `setTimeRange` from the `investigationStore`.
- **Refinement**: Ensure the `displayText` logic in `TimeRangePicker` correctly shows "All Time" if both bounds are empty.

### 2. Cleanup (`FEA-002-UI-002`)
- **Distribution Chart**: Remove the `TimeRangePicker` from the header of `LogDistributionWidget.tsx`.
- **Refactoring**: Ensure the chart still re-renders when the global `timeRange` changes (already handled by the store listener).

### 3. Backend Audit (`FEA-002-BE-001`)
- **api.py**: Check `start_time` and `end_time` logic across all methods (`get_logs`, `get_fused_logs`, `get_log_distribution`).
- **Precision**: Ensure the normalization doesn't strip milliseconds if provided, though DuckDB is fine with just seconds.

## 🧪 Acceptance Tests
- **Criteria 1**: Change time range to "Last 1 hour" -> Table refreshes automatically.
- **Criteria 2**: Open Distribution Chart -> Chart loads data for that 1 hour.
- **Criteria 3**: Toggle Distribution Chart OFF -> Range selection persists in Toolbar.
