# TODO: ui_001
**Title:** Tailwind + Shadcn Foundation
**Status:** Pending

### The Contract (What)
Install Tailwind v4 / Radix base into the React 19 app.

### Execution Steps
1. Run `bunx --bun shadcn-ui@latest init`.
2. Configure `tailwind.config.ts` mapping to Obsidian Lens HSL colors (defined in `SoftwareStandards.md`).
3. Create `src/components/ui/` folder if missing.
