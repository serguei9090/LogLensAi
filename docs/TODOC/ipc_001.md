# TODO: ipc_001
**Title:** Establish Tauri JSON-RPC Bridge
**Status:** Pending

### The Contract (What)
Use `@tauri-apps/plugin-shell` in the React frontend to spawn the Python executable. Establish a `stdin/stdout` streaming listener.

### The Rationale (Why)
This is the central nervous system. Before we write DB logic, the Rust binary must be able to boot the Python sidecar and pass messages asynchronously.

### Execution Steps
1. Add `tauri-plugin-shell` to `src-tauri/Cargo.toml`.
2. Configure `tauri.conf.json` -> `externalBin` to allow sidecars.
3. Write a global React context/hook (`useSidecarBridge`) to listen to stdout and dispatch commands to stdin.
