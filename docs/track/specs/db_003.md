# TODO: db_003
**Title:** DuckDB Search & Filter JSON-RPC Read API
**Status:** Pending

### The Contract (What)
We must establish standard JSON-RPC responder methods in the Python sidecar to accept frontend queries, execute them on DuckDB, and return paginated, structured JSON strings.

### The Rationale (Why)
Currently, `db_002` only ingests logs. Without read methods (e.g., `query_logs(offset=0, limit=100, text="timeout")`), the UI has nothing to display.
