# TODO: ui_004
**Title:** Native File Picker & Search Molecules
**Status:** Pending

### The Contract (What)
Integrate `@tauri-apps/plugin-dialog` to launch OS-native file dialogs allowing the user to select `.log` files to feed into the Python sidecar. Build the Search input molecule to drive `db_003`.

### The Rationale (Why)
A local desktop app shouldn't rely on HTML `<input type="file">` for gigabyte-scale logs. It needs high-performance absolute file paths delivered securely via OS APIs.
