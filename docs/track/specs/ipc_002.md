# TODO: ipc_002
**Title:** Type-Sync Auto-Generator
**Status:** Pending

### The Contract (What)
Set up `pydantic-to-typescript` in the root `package.json` to scrape `sidecar/models.py` and output to `src/lib/types/generated.ts`.

### Execution Steps
1. Create a dummy `sidecar/models.py` with one `TestResource(BaseModel)` object.
2. Run `npm run type-sync` (defined via bun).
3. Verify `generated.ts` is correctly emitted and typed.
