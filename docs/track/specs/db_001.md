# TODO: db_001
**Title:** DuckDB Schema Initialization
**Status:** Pending

### The Contract (What)
Create `sidecar/migrations/001_init.sql` and the Python singleton to apply it on boot.

### Execution Steps
1. Write the SQL defining tables for `Logs` and `Clusters`.
2. Write unit tests inside `sidecar/tests/` using an in-memory db instance.
