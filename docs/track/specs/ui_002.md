# TODO: ui_002
**Title:** Core Layout Organisms
**Status:** Pending

### The Contract (What)
Build `<Sidebar />`, `<Header />`, and `<MetricCard />` using Atomic Design. Connect them to a basic Zustand layout store (Sidebar open/closed state).
