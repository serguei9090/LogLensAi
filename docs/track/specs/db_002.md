# TODO: db_002
**Title:** Drain3 Log Parsing Loop
**Status:** Pending

### The Contract (What)
Initialize the Drain3 log miner, accept raw strings over JSON-RPC, parse them, and INSERT them into DuckDB. Includes performance benchmarks.
